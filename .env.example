# Django Configuration
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database
DATABASE_URL=sqlite:///db.sqlite3

# Email (console backend for dev)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Auth URLs
LOGIN_URL=/auth/login/
LOGIN_REDIRECT_URL=/dashboard/
LOGOUT_REDIRECT_URL=/auth/login/

# GIS Parcelles (embed)
PMTILES_URL=https://cdn.example.com/tiles/osm.pmtiles
PARCELLES_MODE=LITE
JWT_PUBLIC_KEY=dev-secret
JWT_ALGO=HS256

# Urbanisme / PLU / Cadastre
GPU_WFS_BASE=https://wxs-telechargement.geoportail.gouv.fr/<CLE>/wfs
GPU_TYPENAME_ZONES=urba:zone_urba
CADASTRE_WFS_BASE=https://cadastre.data.gouv.fr/wfs
CADASTRE_WFS_TYPENAMES=cadastre:parcelle
CADASTRE_WMS_URL=https://wxs.ign.fr/cadastre/geoportail/r/wms
IGN_WMS_PARCELLAIRE=
IGN_WMS_LAYERS=CADASTRE.PARCELLE,CADASTRE.NUMERO
CACHE_TTL_SECONDS=3600
HTTP_TIMEOUT_SECONDS=6
# Optionnel (orthophoto WMTS si clé)
GEO_WMTS_BASE=https://wxs.ign.fr/<CLE>/geoportail/wmts
